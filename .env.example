# Ollama model to use (must be a Hermes-compatible model)
OLLAMA_MODEL=hermes3:8b

# Port the hermes-agent API listens on
AGENT_PORT=8080

# Ollama API base URL (only change if running Ollama externally)
# OLLAMA_BASE_URL=http://localhost:11434
